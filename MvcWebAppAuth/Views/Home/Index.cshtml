@{
    ViewBag.Title = "Home Page";
}

<div class="row">
    <div class="col-md-6 margin-top-20">
        @*<a href="#" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-circle-arrow-down"> Withdrawal</span></a>
        <a href="#" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-flash"> Quick cash $100</span></a>*@
        <a href="@Url.Action("Details","CheckingAccount", new { checkingAccountId=ViewBag.CheckingAccountId })" class="btn btn-primary btn-lg btn-block">
            <span class="glyphicon glyphicon-question-sign"> Balance Enquiry</span>
        </a>
    </div>
    <div class="col-md-6 margin-top-20">
        @*<a href="#" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-print"> Print statement</span></a>
        <a href="#" class="btn btn-primary btn-lg btn-block"><span class="glyphicon glyphicon-transfer"> Transfer funds</span></a>*@
        <a href="@Url.Action("Create","Transaction",new { checkingAccountId = ViewBag.CheckingAccountId })" class="btn btn-primary btn-lg btn-block">
            <span class="glyphicon glyphicon-circle-arrow-up"> Deposit</span></a>
    </div>

</div>

@*You are using @Request.Browser.Browser !

    @{
        var amounts = new List<Decimal> { 501, 34.34m,- 344.22m, 234.33m };
    }

    <ul>
        @foreach (decimal amt in amounts)
        {
            <li>@formatAmount(amt)</li>
        }
    </ul>

    @helper formatAmount(decimal amt) {
    var color = "green";
    if (amt < 0.0m)
    {
        color = "red";
    }
    <span style="color:@color">@String.Format("{0:c}",amt) </span>

    }*@
